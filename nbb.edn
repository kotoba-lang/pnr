;; nbb.edn -- the kbb engine reads :deps HERE, not from deps.edn (kbb -M:test
;; puts no git coordinate from deps.edn on the classpath). Created by
;; scripts/migrate-clojure-test-to-kotoba-test.cljk; :paths copied from deps.edn.
{:paths ["src" "test"]
 :deps {
        ;; kotoba.lang.coll replaced clojure.lang.PersistentQueue here (2026-09-25).
        ;; An explicit floor: tools.deps takes the NEWEST sha it is shown.
        io.github.kotoba-lang/coll {:git/sha "85a124b943db2c068de25e1bc5eb51881d191a84"}
        ;; kotoba.test (clojure.test-compatible surface), not clojure.test --
        ;; scripts/migrate-clojure-test-to-kotoba-test.cljk. Explicit floor.
        io.github.kotoba-lang/test {:git/url "https://github.com/kotoba-lang/test.git"
                                    :git/sha "2ab4885f99fd25552d380d0f6b0b54c5a72eb7ae"}}}
